spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: application
      profile: datasource,eureka-client,friend
logging:
  level:
    com:
      stan:
        mapper: debug
mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.stan.entity

